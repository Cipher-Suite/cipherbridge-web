# Gateway URL — your Tonpo Gateway
VITE_GATEWAY_URL=https://gateway.cipherbridge.cloud

# (billing is already proxied through gateway):
# Billing service base URL (public endpoint, no API key needed).
# Billing API calls are proxied through the gateway — no API key required here.
